# Copy to .env in the project root: cp .env.example .env

MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=plasticflow
MYSQL_USER=root
MYSQL_PASSWORD=rootpass
MYSQL_PORT=3306

# Flask backend (used inside Docker Compose)
MYSQL_HOST=mysql
